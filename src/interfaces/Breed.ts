export interface Breed {
  id: string
  name: string
  minHeight: number
  maxHeight: number
  minWeight: number
  maxWeight: number
  minPrice: number
  maxPrice: number
  lifeSpan: number
  purpose: string
}
